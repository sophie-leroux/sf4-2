{# templates/includes/flashes.html.twig #}

{% for type, messages in app.flashes %}
    {% for message in messages %}
        <div class="notification is-{{ type }}">
            {{ message }}
        </div>
        {% endfor %}
{% endfor %}

{#

    [
        'success' => ['msg1', 'msg2'],
        'danger' => ['msg1'],
    ]

#}